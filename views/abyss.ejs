<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>アビス探窟家リスト</title>
</head>
<body>
  <h2>探窟家図鑑</h2>
  <table>
    <tr><th>ID</th><th>名前</th><th>ランク/種族</th></tr>
      <% let number = 0 %>
      <% for( let row of data ) { %>
        <tr>
          <td><%= row.id %></td>
          <td><a href="/abyss/<%= number %>"><%= row.name %></a></td>
          <td><%= row.rank %></td>
        </tr>
        <% number += 1 %>
      <% } %>
  </table>
  <hr>
  <a href="/abyss/create">新規探窟家を登録</a>
</body>
</html>